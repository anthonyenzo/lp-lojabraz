name: Publicar loja na Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-hostinger-loja
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-deploy:
    name: Build e deploy via FTP
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: production
    env:
      HOSTINGER_BUILD: "1"
      NEXT_PUBLIC_DEPLOY_BASE_PATH: /loja

    steps:
      - name: Baixar o código
        uses: actions/checkout@v6
      - name: Preparar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11
      - name: Preparar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - name: Instalar dependências
        run: pnpm install --frozen-lockfile
      - name: Gerar site estático
        run: pnpm run build:hostinger
      - name: Conferir arquivos antes do envio
        run: test -f out/index.html && test -d out/_next
      - name: Publicar somente /loja/
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.HOSTINGER_FTP_SERVER }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./out/
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          state-name: .brazhits-loja-sync.json
          dangerous-clean-slate: false
          log-level: minimal
